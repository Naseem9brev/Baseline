# Baseline — optional build-time defaults (prefer Settings tab in the extension).
# User API keys should be entered in the side panel → Settings (stored in chrome.storage.local).

# Not used when keys are saved via Settings UI:
# VITE_ZAI_API_KEY=
# VITE_GEMINI_API_KEY=
# VITE_ELEVENLABS_API_KEY=
